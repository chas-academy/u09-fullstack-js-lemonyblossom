import LogChart from './LogChart'; // Import the LogChart component

const Stats = () => {
  return (
    <div className="stats-container">
      <h1>Statistics</h1>
      <LogChart /> 
    </div>
  );
};

export default Stats;
